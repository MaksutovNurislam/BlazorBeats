<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Мои загрузки - BeatMarket</title>
    <link rel="stylesheet" href="style.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <header>
        <div class="container">
            <div class="header-content">
                <div class="logo">
                    <i class="fas fa-wave-square"></i>
                    <span>BeatMarket</span>
                </div>
                <nav>
                    <ul>
                        <li><a href="index.html">Биты</a></li>
                        <li><a href="profile.html">Профиль</a></li>
                        <li><a href="downloads.html" class="active">Загрузки</a></li>
                    </ul>
                </nav>
                <div class="user-actions">
                    <div class="balance">1,250 ₽</div>
                    <div class="avatar">
                        <i class="fas fa-user"></i>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <main>
        <div class="container">
            <div class="downloads-header">
                <h1>Мои загрузки</h1>
                <p>Все приобретенные биты доступны для скачивания</p>
                <div class="downloads-stats">
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="fas fa-download"></i>
                        </div>
                        <div class="stat-info">
                            <div class="stat-value">24</div>
                            <div class="stat-label">Всего загрузок</div>
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="fas fa-music"></i>
                        </div>
                        <div class="stat-info">
                            <div class="stat-value">18.5 GB</div>
                            <div class="stat-label">Общий объем</div>
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="fas fa-crown"></i>
                        </div>
                        <div class="stat-info">
                            <div class="stat-value">Premium</div>
                            <div class="stat-label">Ваш статус</div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="downloads-controls">
                <div class="search-downloads">
                    <input type="text" placeholder="Поиск в загрузках...">
                    <button><i class="fas fa-search"></i></button>
                </div>
                <div class="filter-controls">
                    <select>
                        <option>Все форматы</option>
                        <option>WAV</option>
                        <option>MP3</option>
                        <option>STEMS</option>
                    </select>
                    <select>
                        <option>Сначала новые</option>
                        <option>Сначала старые</option>
                        <option>По названию</option>
                    </select>
                </div>
            </div>

            <div class="downloads-list">
                <div class="download-item">
                    <div class="download-preview">
                        <div class="play-button">
                            <i class="fas fa-play"></i>
                        </div>
                    </div>
                    <div class="download-info">
                        <h3>Midnight Dreams</h3>
                        <p class="producer">Prod. by WaveMaker</p>
                        <div class="download-meta">
                            <span class="file-format">WAV • 24-bit • 44.1kHz</span>
                            <span class="file-size">185 MB</span>
                            <span class="purchase-date">Куплено: 15.03.2024</span>
                        </div>
                    </div>
                    <div class="download-actions">
                        <button class="btn-download">
                            <i class="fas fa-download"></i>
                            Скачать
                        </button>
                        <div class="format-options">
                            <button class="format-btn active">WAV</button>
                            <button class="format-btn">MP3</button>
                            <button class="format-btn">STEMS</button>
                        </div>
                    </div>
                </div>

                <div class="download-item">
                    <div class="download-preview">
                        <div class="play-button">
                            <i class="fas fa-play"></i>
                        </div>
                    </div>
                    <div class="download-info">
                        <h3>Dark Alley</h3>
                        <p class="producer">Prod. by ShadowBeats</p>
                        <div class="download-meta">
                            <span class="file-format">WAV • 24-bit • 48kHz</span>
                            <span class="file-size">210 MB</span>
                            <span class="purchase-date">Куплено: 10.03.2024</span>
                        </div>
                    </div>
                    <div class="download-actions">
                        <button class="btn-download">
                            <i class="fas fa-download"></i>
                            Скачать
                        </button>
                        <div class="format-options">
                            <button class="format-btn active">WAV</button>
                            <button class="format-btn">MP3</button>
                            <button class="format-btn">STEMS</button>
                        </div>
                    </div>
                </div>

                <div class="download-item">
                    <div class="download-preview">
                        <div class="play-button">
                            <i class="fas fa-play"></i>
                        </div>
                    </div>
                    <div class="download-info">
                        <h3>Ocean Breeze</h3>
                        <p class="producer">Prod. by MelodyMaster</p>
                        <div class="download-meta">
                            <span class="file-format">WAV • 24-bit • 44.1kHz</span>
                            <span class="file-size">165 MB</span>
                            <span class="purchase-date">Куплено: 05.03.2024</span>
                        </div>
                    </div>
                    <div class="download-actions">
                        <button class="btn-download">
                            <i class="fas fa-download"></i>
                            Скачать
                        </button>
                        <div class="format-options">
                            <button class="format-btn active">WAV</button>
                            <button class="format-btn">MP3</button>
                            <button class="format-btn">STEMS</button>
                        </div>
                    </div>
                </div>

                <div class="download-item">
                    <div class="download-preview">
                        <div class="play-button">
                            <i class="fas fa-play"></i>
                        </div>
                    </div>
                    <div class="download-info">
                        <h3>Urban Legend</h3>
                        <p class="producer">Prod. by CitySounds</p>
                        <div class="download-meta">
                            <span class="file-format">WAV • 24-bit • 44.1kHz</span>
                            <span class="file-size">195 MB</span>
                            <span class="purchase-date">Куплено: 28.02.2024</span>
                        </div>
                    </div>
                    <div class="download-actions">
                        <button class="btn-download">
                            <i class="fas fa-download"></i>
                            Скачать
                        </button>
                        <div class="format-options">
                            <button class="format-btn active">WAV</button>
                            <button class="format-btn">MP3</button>
                            <button class="format-btn">STEMS</button>
                        </div>
                    </div>
                </div>

                <div class="download-item">
                    <div class="download-preview">
                        <div class="play-button">
                            <i class="fas fa-play"></i>
                        </div>
                    </div>
                    <div class="download-info">
                        <h3>Sunset Drive</h3>
                        <p class="producer">Prod. by TropicalBeats</p>
                        <div class="download-meta">
                            <span class="file-format">WAV • 24-bit • 48kHz</span>
                            <span class="file-size">205 MB</span>
                            <span class="purchase-date">Куплено: 20.02.2024</span>
                        </div>
                    </div>
                    <div class="download-actions">
                        <button class="btn-download">
                            <i class="fas fa-download"></i>
                            Скачать
                        </button>
                        <div class="format-options">
                            <button class="format-btn active">WAV</button>
                            <button class="format-btn">MP3</button>
                            <button class="format-btn">STEMS</button>
                        </div>
                    </div>
                </div>
            </div>

            <div class="downloads-footer">
                <div class="storage-info">
                    <h4>Использование хранилища</h4>
                    <div class="storage-bar">
                        <div class="storage-progress" style="width: 65%"></div>
                    </div>
                    <p>18.5 GB из 28.4 GB использовано</p>
                </div>
                <button class="btn-secondary">
                    <i class="fas fa-cloud-download-alt"></i>
                    Скачать все в ZIP
                </button>
            </div>
        </div>
    </main>

    <footer>
        <div class="container">
            <p>&copy; 2024 BeatMarket. Все права защищены.</p>
        </div>
    </footer>

    <script>
        // Проигрывание битов
        document.querySelectorAll('.play-button').forEach(button => {
            button.addEventListener('click', function() {
                const isPlaying = this.classList.contains('playing');
                
                // Останавливаем все другие проигрывания
                document.querySelectorAll('.play-button').forEach(btn => {
                    btn.classList.remove('playing');
                    btn.innerHTML = '<i class="fas fa-play"></i>';
                });
                
                if (!isPlaying) {
                    this.classList.add('playing');
                    this.innerHTML = '<i class="fas fa-pause"></i>';
                }
            });
        });

        // Выбор формата
        document.querySelectorAll('.format-btn').forEach(button => {
            button.addEventListener('click', function() {
                this.parentElement.querySelectorAll('.format-btn').forEach(btn => {
                    btn.classList.remove('active');
                });
                this.classList.add('active');
            });
        });

        // Скачивание
        document.querySelectorAll('.btn-download').forEach(button => {
            button.addEventListener('click', function() {
                const originalText = this.innerHTML;
                this.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Скачивание...';
                this.disabled = true;
                
                setTimeout(() => {
                    this.innerHTML = '<i class="fas fa-check"></i> Скачано!';
                    setTimeout(() => {
                        this.innerHTML = originalText;
                        this.disabled = false;
                    }, 2000);
                }, 1500);
            });
        });
    </script>
</body>
</html>
<style>
:root {
    --primary: #8b5cf6;
    --primary-dark: #7c3aed;
    --primary-light: #a78bfa;
    --secondary: #f59e0b;
    --accent: #10b981;
    --dark: #0f0f23;
    --darker: #090915;
    --dark-light: #1a1a2e;
    --card-bg: #161627;
    --text: #e2e8f0;
    --text-light: #94a3b8;
    --text-lighter: #64748b;
    --border: #2d2d42;
    --shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
    --radius: 16px;
    --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Inter', 'Segoe UI', system-ui, sans-serif;
}

body {
    background: linear-gradient(135deg, var(--darker) 0%, var(--dark) 100%);
    color: var(--text);
    line-height: 1.6;
    min-height: 100vh;
}

.container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 20px;
}

/* Header */
header {
    background: rgba(15, 15, 35, 0.9);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--border);
    position: sticky;
    top: 0;
    z-index: 1000;
}

.header-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 0;
}

.logo {
    font-size: 28px;
    font-weight: 800;
    background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    display: flex;
    align-items: center;
    gap: 12px;
}

.logo i {
    font-size: 32px;
}

nav ul {
    display: flex;
    list-style: none;
    gap: 40px;
}

nav ul li a {
    text-decoration: none;
    color: var(--text-light);
    font-weight: 600;
    transition: var(--transition);
    padding: 12px 0;
    position: relative;
    font-size: 16px;
}

nav ul li a:hover,
nav ul li a.active {
    color: var(--text);
}

nav ul li a.active::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 3px;
    background: linear-gradient(90deg, var(--primary), var(--secondary));
    border-radius: 2px;
}

.user-actions {
    display: flex;
    align-items: center;
    gap: 20px;
}

.balance {
    background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
    color: white;
    padding: 10px 20px;
    border-radius: 25px;
    font-weight: 700;
    font-size: 15px;
    box-shadow: var(--shadow);
}

.avatar {
    width: 48px;
    height: 48px;
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 20px;
    cursor: pointer;
    transition: var(--transition);
}

.avatar:hover {
    transform: scale(1.05);
}

/* Main Content */
main {
    padding: 40px 0;
}

/* Hero Section */
.hero {
    text-align: center;
    padding: 80px 0 60px;
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.1) 0%, rgba(15, 15, 35, 0.8) 100%);
    border-radius: var(--radius);
    margin-bottom: 50px;
    position: relative;
    overflow: hidden;
}

.hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at top right, rgba(139, 92, 246, 0.15), transparent 70%);
}

.hero h1 {
    font-size: 3.5rem;
    font-weight: 800;
    margin-bottom: 20px;
    background: linear-gradient(135deg, var(--text) 0%, var(--primary-light) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    position: relative;
}

.hero p {
    font-size: 1.3rem;
    color: var(--text-light);
    margin-bottom: 40px;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
}

.search-bar {
    display: flex;
    max-width: 600px;
    margin: 0 auto;
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 50px;
    overflow: hidden;
    box-shadow: var(--shadow);
}

.search-bar input {
    flex: 1;
    background: transparent;
    border: none;
    padding: 20px 30px;
    color: var(--text);
    font-size: 16px;
    outline: none;
}

.search-bar input::placeholder {
    color: var(--text-lighter);
}

.search-bar button {
    background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
    border: none;
    padding: 0 30px;
    color: white;
    cursor: pointer;
    transition: var(--transition);
    font-size: 18px;
}

.search-bar button:hover {
    background: linear-gradient(135deg, var(--primary-dark) 0%, var(--primary) 100%);
}

/* Filters */
.filters {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 40px;
    flex-wrap: wrap;
    gap: 20px;
}

.filter-tags {
    display: flex;
    gap: 15px;
    flex-wrap: wrap;
}

.filter-tag {
    padding: 12px 24px;
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 25px;
    color: var(--text-light);
    cursor: pointer;
    transition: var(--transition);
    font-weight: 600;
    font-size: 14px;
}

.filter-tag:hover,
.filter-tag.active {
    background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
    color: white;
    border-color: var(--primary);
    transform: translateY(-2px);
}

.sort select {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 12px 20px;
    color: var(--text);
    font-size: 14px;
    cursor: pointer;
    outline: none;
}

/* Beats Grid */
.beats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(380px, 1fr));
    gap: 30px;
    margin-bottom: 60px;
}

.beat-card {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    overflow: hidden;
    transition: var(--transition);
    position: relative;
}

.beat-card:hover {
    transform: translateY(-8px);
    border-color: var(--primary);
    box-shadow: var(--shadow);
}

.beat-card.featured {
    border: 2px solid var(--secondary);
}

.featured-badge {
    position: absolute;
    top: 15px;
    right: 15px;
    background: linear-gradient(135deg, var(--secondary) 0%, #f97316 100%);
    color: white;
    padding: 6px 15px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 700;
    z-index: 2;
}

.beat-preview {
    position: relative;
    height: 140px;
    background: linear-gradient(135deg, var(--dark-light) 0%, var(--card-bg) 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
}

.play-button {
    width: 60px;
    height: 60px;
    background: rgba(255, 255, 255, 0.95);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--primary-dark);
    font-size: 20px;
    transition: var(--transition);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
}

.play-button:hover,
.play-button.playing {
    background: var(--primary);
    color: white;
    transform: scale(1.1);
}


.beat-info {
    padding: 25px;
}

.beat-info h3 {
    font-size: 1.4rem;
    font-weight: 700;
    margin-bottom: 8px;
    color: var(--text);
}

.producer {
    color: var(--primary-light);
    font-weight: 600;
    margin-bottom: 15px;
    font-size: 14px;
}

.tags {
    display: flex;
    gap: 10px;
    margin-bottom: 20px;
}

.tag {
    background: rgba(139, 92, 246, 0.1);
    color: var(--primary-light);
    padding: 6px 12px;
    border-radius: 15px;
    font-size: 12px;
    font-weight: 600;
    border: 1px solid rgba(139, 92, 246, 0.3);
}

.beat-meta {
    display: flex;
    gap: 20px;
    color: var(--text-light);
    font-size: 14px;
}

.beat-meta span {
    display: flex;
    align-items: center;
    gap: 6px;
}

.beat-actions {
    padding: 0 25px 25px;
    display: flex;
    align-items: center;
    gap: 15px;
}

.price {
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--text);
    flex: 1;
}

.btn-primary {
    background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
    color: white;
    border: none;
    padding: 14px 28px;
    border-radius: 12px;
    font-weight: 700;
    cursor: pointer;
    transition: var(--transition);
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 15px;
}

.btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(139, 92, 246, 0.4);
}

.btn-secondary {
    background: var(--dark-light);
    color: var(--text);
    border: 1px solid var(--border);
    padding: 14px;
    border-radius: 12px;
    font-weight: 600;
    cursor: pointer;
    transition: var(--transition);
    display: flex;
    align-items: center;
    justify-content: center;
}

.btn-secondary:hover,
.btn-secondary.in-cart {
    background: var(--primary);
    color: white;
    border-color: var(--primary);
}

/* Footer */
footer {
    background: var(--darker);
    border-top: 1px solid var(--border);
    padding: 30px 0;
    text-align: center;
    color: var(--text-light);
    margin-top: 60px;
}

/* Profile Page Styles */
.profile-header {
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: 40px;
    align-items: center;
    background: var(--card-bg);
    border-radius: var(--radius);
    padding: 40px;
    margin-bottom: 40px;
    border: 1px solid var(--border);
}

.profile-avatar {
    position: relative;
}

.avatar-large {
    width: 120px;
    height: 120px;
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 40px;
}

.btn-edit {
    position: absolute;
    bottom: 5px;
    right: 5px;
    width: 36px;
    height: 36px;
    background: var(--dark-light);
    border: 2px solid var(--card-bg);
    border-radius: 50%;
    color: var(--text);
    cursor: pointer;
    transition: var(--transition);
}

.btn-edit:hover {
    background: var(--primary);
    color: white;
}

.profile-info h1 {
    font-size: 2.5rem;
    font-weight: 800;
    margin-bottom: 10px;
    background: linear-gradient(135deg, var(--text) 0%, var(--primary-light) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.profile-bio {
    color: var(--text-light);
    font-size: 1.1rem;
    margin-bottom: 30px;
}

.profile-stats {
    display: flex;
    gap: 40px;
}

.stat {
    text-align: center;
}

.stat-value {
    font-size: 2rem;
    font-weight: 800;
    color: var(--primary);
    margin-bottom: 5px;
}

.stat-label {
    color: var(--text-light);
    font-size: 14px;
}

.profile-actions {
    display: flex;
    flex-direction: column;
    gap: 15px;
}

.profile-content {
    display: grid;
    grid-template-columns: 300px 1fr;
    gap: 30px;
}

.sidebar-section {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 25px;
    margin-bottom: 25px;
}

.sidebar-section h3 {
    font-size: 1.2rem;
    margin-bottom: 20px;
    color: var(--text);
    font-weight: 700;
}

.balance-card {
    text-align: center;
}

.balance-amount {
    font-size: 2.2rem;
    font-weight: 800;
    color: var(--primary);
    margin-bottom: 20px;
}

.cart-item {
    display: flex;
    justify-content: space-between;
    padding: 15px 0;
    border-bottom: 1px solid var(--border);
}

.preferences {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
}

.preference-tag {
    background: rgba(139, 92, 246, 0.1);
    color: var(--primary-light);
    padding: 8px 16px;
    border-radius: 20px;
    font-size: 13px;
    font-weight: 600;
    border: 1px solid rgba(139, 92, 246, 0.3);
}

.tabs {
    display: flex;
    gap: 5px;
    background: var(--dark-light);
    padding: 8px;
    border-radius: 12px;
    margin-bottom: 30px;
}

.tab-button {
    flex: 1;
    background: transparent;
    border: none;
    padding: 15px 20px;
    border-radius: 8px;
    color: var(--text-light);
    font-weight: 600;
    cursor: pointer;
    transition: var(--transition);
}

.tab-button.active {
    background: var(--primary);
    color: white;
}

.favorites-grid {
    display: grid;
    gap: 20px;
}

.favorite-item {
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: 20px;
    align-items: center;
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 20px;
    transition: var(--transition);
}

.favorite-item:hover {
    border-color: var(--primary);
    transform: translateX(5px);
}

.favorite-preview {
    display: flex;
    align-items: center;
    gap: 15px;
}

.play-button.small {
    width: 40px;
    height: 40px;
    font-size: 14px;
}

.waveform.small {
    position: static;
    width: 80px;
    height: 20px;
}

.favorite-info h4 {
    font-size: 1.1rem;
    font-weight: 700;
    margin-bottom: 5px;
}

.favorite-info p {
    color: var(--text-light);
    font-size: 14px;
    margin-bottom: 8px;
}

.favorite-info .price {
    font-size: 1.2rem;
    color: var(--primary);
}

.favorite-actions {
    display: flex;
    gap: 10px;
    align-items: center;
}

.btn-remove {
    background: transparent;
    border: 1px solid var(--border);
    color: var(--text-light);
    width: 40px;
    height: 40px;
    border-radius: 10px;
    cursor: pointer;
    transition: var(--transition);
    display: flex;
    align-items: center;
    justify-content: center;
}

.btn-remove:hover {
    background: #ef4444;
    border-color: #ef4444;
    color: white;
}

/* Downloads Page Styles */
.downloads-header {
    text-align: center;
    padding: 60px 0 40px;
}

.downloads-header h1 {
    font-size: 3rem;
    font-weight: 800;
    margin-bottom: 15px;
    background: linear-gradient(135deg, var(--text) 0%, var(--primary-light) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.downloads-header p {
    font-size: 1.2rem;
    color: var(--text-light);
    margin-bottom: 50px;
}

.downloads-stats {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 25px;
    margin-bottom: 50px;
}

.stat-card {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 30px;
    display: flex;
    align-items: center;
    gap: 20px;
    transition: var(--transition);
}

.stat-card:hover {
    border-color: var(--primary);
    transform: translateY(-5px);
}

.stat-icon {
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
    border-radius: 15px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 24px;
}

.stat-info .stat-value {
    font-size: 1.8rem;
    font-weight: 800;
    color: var(--text);
    margin-bottom: 5px;
}

.stat-info .stat-label {
    color: var(--text-light);
    font-size: 14px;
}

.downloads-controls {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 30px;
    flex-wrap: wrap;
    gap: 20px;
}

.search-downloads {
    display: flex;
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 12px;
    overflow: hidden;
    flex: 1;
    max-width: 400px;
}

.search-downloads input {
    flex: 1;
    background: transparent;
    border: none;
    padding: 15px 20px;
    color: var(--text);
    outline: none;
}

.search-downloads button {
    background: var(--dark-light);
    border: none;
    padding: 15px 20px;
    color: var(--text-light);
    cursor: pointer;
    transition: var(--transition);
}

.search-downloads button:hover {
    background: var(--primary);
    color: white;
}

.filter-controls {
    display: flex;
    gap: 15px;
}

.filter-controls select {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 15px 20px;
    color: var(--text);
    cursor: pointer;
    outline: none;
    min-width: 180px;
}

.downloads-list {
    display: flex;
    flex-direction: column;
    gap: 20px;
    margin-bottom: 50px;
}

.download-item {
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: 25px;
    align-items: center;
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 25px;
    transition: var(--transition);
}

.download-item:hover {
    border-color: var(--primary);
    transform: translateX(5px);
}

.download-preview {
    display: flex;
    align-items: center;
    gap: 20px;
}

.download-info h3 {
    font-size: 1.3rem;
    font-weight: 700;
    margin-bottom: 8px;
    color: var(--text);
}

.download-info .producer {
    color: var(--primary-light);
    font-weight: 600;
    margin-bottom: 15px;
    font-size: 14px;
}

.download-meta {
    display: flex;
    gap: 25px;
    color: var(--text-light);
    font-size: 14px;
}

.file-format {
    color: var(--accent);
    font-weight: 600;
}

.download-actions {
    display: flex;
    flex-direction: column;
    gap: 15px;
    align-items: end;
}

.btn-download {
    background: linear-gradient(135deg, var(--accent) 0%, #059669 100%);
    color: white;
    border: none;
    padding: 12px 25px;
    border-radius: 10px;
    font-weight: 700;
    cursor: pointer;
    transition: var(--transition);
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 14px;
}

.btn-download:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(16, 185, 129, 0.4);
}

.format-options {
    display: flex;
    gap: 8px;
    background: var(--dark-light);
    padding: 6px;
    border-radius: 10px;
}

.format-btn {
    background: transparent;
    border: none;
    padding: 8px 16px;
    border-radius: 8px;
    color: var(--text-light);
    font-size: 12px;
    font-weight: 600;
    cursor: pointer;
    transition: var(--transition);
}

.format-btn.active {
    background: var(--primary);
    color: white;
}

.downloads-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 30px;
    flex-wrap: wrap;
    gap: 20px;
}

.storage-info h4 {
    font-size: 1.1rem;
    margin-bottom: 15px;
    color: var(--text);
}

.storage-bar {
    width: 300px;
    height: 8px;
    background: var(--dark-light);
    border-radius: 4px;
    overflow: hidden;
    margin-bottom: 10px;
}

.storage-progress {
    height: 100%;
    background: linear-gradient(90deg, var(--primary), var(--secondary));
    border-radius: 4px;
    transition: width 0.5s ease;
}

.storage-info p {
    color: var(--text-light);
    font-size: 14px;
}

/* Responsive Design */
@media (max-width: 1024px) {
    .beats-grid {
        grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
    }
    
    .profile-content {
        grid-template-columns: 1fr;
    }
    
    .profile-header {
        grid-template-columns: 1fr;
        text-align: center;
        gap: 30px;
    }
    
    .profile-stats {
        justify-content: center;
    }
}

@media (max-width: 768px) {
    .header-content {
        flex-direction: column;
        gap: 20px;
    }
    
    nav ul {
        gap: 20px;
    }
    
    .hero h1 {
        font-size: 2.5rem;
    }
    
    .beats-grid {
        grid-template-columns: 1fr;
    }
    
    .filters {
        flex-direction: column;
        align-items: stretch;
    }
    
    .download-item {
        grid-template-columns: 1fr;
        text-align: center;
    }
    
    .download-actions {
        align-items: center;
    }
}

@media (max-width: 480px) {
    .container {
        padding: 0 15px;
    }
    
    .hero {
        padding: 50px 0 40px;
    }
    
    .hero h1 {
        font-size: 2rem;
    }
    
    .beat-actions {
        flex-direction: column;
        align-items: stretch;
    }
    
    .profile-stats {
        flex-direction: column;
        gap: 20px;
    }
}</style>